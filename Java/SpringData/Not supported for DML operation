add @Modifying

    @Modifying
    @Query("update TeClient set observations = :observations where id=:idClient")
    void updateObservations(@Param("idClient") Integer idClient, @Param("observations") String observations);
    
    
    Note: add @Transactional too
