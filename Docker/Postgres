docker search postgres  // search postgres images
docker pull postgres    // get postgres official image

docker images           // check local images

docker run --name postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
docker run CONTAINER NAME -e DEFAULT PASSWORD -p PORTS -d DELETE AFTER DOWN postgres(IMAGE NAME)

psql -U postgres -h localhost // password postgres SET in previous commande
