mkdir -p /complete/path
ls -larth
chown -R ywang:ywang folder
du -sh FOLDER // check folder size
du -h --max-depth=1 // check folder size

df -h // check disk usage

tcpdump -i any port 1521 or port 636 and host www.google.fr -A -s0
-s0 don't truncate display
-A display on terminal
-w write in a file like file.pcap (readable by Wireshark)
-i interface (eth0, any, ...)


resize photos
sudo apt install imagemagick
ls *.jpg | xargs -n1 sh -c 'convert $0 -geometry 1024x resized/$0'
